<%= form_for :waterfall, url: waterfalls_path do |f| %>

  <% if @waterfall.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@waterfall.errors.count, "error") %> prohibited
        this waterfall from being saved:
      </h2>
      <ul>
        <% @waterfall.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text, size: "60x10" %>
  </p>
 
  <p>
    <%= f.submit %>
  </p><br>
<% end %>